import {View, Text, Modal} from 'react-native';
import React from 'react';

const StartLive = ({liveStartVisible}) => {
  return <Modal visible={liveStartVisible}></Modal>;
};

export default StartLive;
